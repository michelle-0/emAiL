@model IEnumerable<EfFuncCallSK.Models.ChatHistory>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.UserMessage)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AIResponse)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Timestamp)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.UserMessage)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AIResponse)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Timestamp)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ChatId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ChatId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ChatId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
